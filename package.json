{
  "name": "navee-tuner",
  "version": "1.0.0",
  "description": "Navee ST3 Pro Tuner",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "cap:sync": "npx cap sync",
    "cap:build": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor-community/bluetooth-le": "^3.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
